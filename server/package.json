{
  "name": "refhub-server",
  "version": "1.0.0",
  "private": true,
  "description": "RefHub server component",
  "scripts": {
    "start": "NODE_ENV=production node dist/index.js",
    "dev": "NODE_ENV=development tsx standalone-server.ts",
    "build": "esbuild standalone-server.ts --platform=node --packages=external --bundle --format=esm --outdir=../dist/server"
  },
  "dependencies": {
    "@octokit/rest": "^21.1.1",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "lowdb": "^7.0.1",
    "memorystore": "^1.6.7",
    "passport": "^0.7.0",
    "passport-local": "^1.0.0",
    "zod": "^3.24.2",
    "zod-validation-error": "^3.4.0"
  }
}