# Client Environment Variables for Production

# API URL is empty - this means we're using static JSON files
VITE_API_URL=

# GitHub Pages mode flag (true for production)
VITE_GITHUB_PAGES=true

# Base path will be set by the GitHub workflow
# Default: repository name with leading and trailing slashes
VITE_BASE_PATH=/

# Custom domain flag (will be set by the GitHub workflow)
VITE_USE_CUSTOM_DOMAIN=false