# Client Environment Variables for Development

# API URL for development (when running client and server separately)
VITE_API_URL=http://localhost:5000

# GitHub Pages mode flag (false for development)
VITE_GITHUB_PAGES=false

# Base path for the application (default for development)
VITE_BASE_PATH=/

# Custom domain flag (false for development)
VITE_USE_CUSTOM_DOMAIN=false

# Client development port (default Vite port)
PORT=3000