# Client Environment Variables for Production

# API URL is empty in production for GitHub Pages deployment
VITE_API_URL=

# GitHub Pages mode flag (true for production)
VITE_GITHUB_PAGES=true

# Base path for the application (should be set by GitHub workflow)
# For a repository named "refhub", this would be /refhub/
VITE_BASE_PATH=/refhub/

# Custom domain flag (set to true if using a custom domain)
VITE_USE_CUSTOM_DOMAIN=false